#include "main.h"
/**
 * is_prime_number - func for prime number
 * @n: given number
 * Return: an int/ the result
 */
int is_prime_number(int n)
{
	if (y < 0)
	{
		return (-1);
	}
	else if (y == 0)
	{
		return (1);
	}
	else
	{
		return (x * _pow_recursion(x, y - 1));
	}
}
